// Callisto - callisto-core <https://github.com/msikma/callisto>
// © MIT license

/**
 * Runs the actual syntax checks. Returns an object containing
 * information on what parts of the syntax are malformed.
 */
const runCacheCheck = (path) => {
  return {
    success: true
  }
}

module.exports = {
  checkConfigProps
}
