// Callisto - callisto-core <https://github.com/msikma/callisto>
// © MIT license

/**
 * Initializes the cache directory and database.
 * 
 *   1) verifies the cache directory exists and is writeable
 *   2) connects to the cache database and ensures its tables are set up properly
 *   3) 
 */
const initCache$ = async (pathCache) => {
  // Check cache directory.
  console.log(pathCache);
  process.exit(0)
}

module.exports = initCache$
